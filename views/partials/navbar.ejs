<nav class="navbar">
    <div class="nav-container">
        <a href="/" class="nav-logo">
            <img src="/images/logo.png" alt="HKMC">
            <span>HKMC</span>
        </a>

        <div class="menu-toggle" id="mobile-menu">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </div>

        <ul class="nav-menu">
            <li><a href="/" class="nav-link <%= page === 'home' ? 'active' : '' %>">Home</a></li>
            <li><a href="/leaderboard" class="nav-link <%= page === 'leaderboard' ? 'active' : '' %>">Stats</a></li>
            <li><a href="/store" class="nav-link <%= page === 'store' ? 'active' : '' %>" style="color: #ffd700;">Store 💎</a></li>
            <li><a href="/vote" class="nav-link <%= page === 'vote' ? 'active' : '' %>">Vote</a></li>
            
            <li class="mobile-btn">
                <a href="https://discord.gg/yourinvite" class="discord-btn-mobile">Join Discord</a>
            </li>
        </ul>

        <a href="https://discord.gg/yourinvite" target="_blank" class="discord-btn desktop-btn">
            <i class="fab fa-discord"></i> Join Discord
        </a>
    </div>
</nav>

<script>
    // Script to toggle the menu
    const menu = document.querySelector('#mobile-menu');
    const navMenu = document.querySelector('.nav-menu');

    menu.addEventListener('click', () => {
        menu.classList.toggle('is-active');
        navMenu.classList.toggle('active');
    });
</script>

<style>
    /* --- NAVBAR CSS --- */
    .navbar {
        background: rgba(5, 5, 5, 0.95);
        height: 70px;
        display: flex;
        justify-content: center;
        align-items: center;
        font-size: 1rem;
        position: sticky;
        top: 0;
        z-index: 999;
        border-bottom: 1px solid rgba(255, 0, 0, 0.2);
        backdrop-filter: blur(10px);
    }

    .nav-container {
        display: flex;
        justify-content: space-between;
        height: 70px;
        z-index: 1;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
        align-items: center;
    }

    .nav-logo {
        color: #fff;
        display: flex;
        align-items: center;
        cursor: pointer;
        text-decoration: none;
        font-size: 1.5rem;
        font-weight: 800;
    }

    .nav-logo img { width: 40px; margin-right: 10px; }

    .nav-menu {
        display: flex;
        align-items: center;
        list-style: none;
        text-align: center;
        gap: 20px;
    }

    .nav-link {
        color: #fff;
        display: flex;
        align-items: center;
        text-decoration: none;
        padding: 0 1rem;
        height: 100%;
        font-weight: 500;
        transition: 0.3s;
    }

    .nav-link:hover, .nav-link.active {
        color: #ff1a1a;
    }

    .discord-btn {
        background: #5865F2;
        padding: 8px 20px;
        border-radius: 4px;
        color: white;
        text-decoration: none;
        font-weight: bold;
        transition: 0.3s;
    }
    .discord-btn:hover { background: #4752c4; }

    .mobile-btn { display: none; }

    /* --- MOBILE RESPONSIVENESS --- */
    @media screen and (max-width: 960px) {
        .nav-menu {
            display: flex;
            flex-direction: column;
            width: 100%;
            height: 90vh; /* Full screen menu */
            position: absolute;
            top: 70px; /* Below navbar */
            left: -100%; /* Hidden by default */
            opacity: 1;
            transition: all 0.5s ease;
            background: #000;
            padding-top: 30px;
            border-top: 1px solid #222;
        }

        .nav-menu.active {
            left: 0; /* Slide in */
        }

        .nav-link {
            text-align: center;
            padding: 2rem;
            width: 100%;
            display: table;
            font-size: 1.5rem;
        }

        .desktop-btn { display: none; }
        
        .mobile-btn {
            display: block;
            width: 100%;
            text-align: center;
            padding-bottom: 2rem;
        }

        .discord-btn-mobile {
            background: #5865F2;
            padding: 15px 60px;
            border-radius: 4px;
            color: white;
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: bold;
        }

        /* Hamburger Icon Animation */
        .menu-toggle {
            display: block;
            cursor: pointer;
        }

        .menu-toggle .bar {
            display: block;
            width: 25px;
            height: 3px;
            margin: 5px auto;
            transition: all 0.3s ease-in-out;
            background: #fff;
        }

        .menu-toggle.is-active .bar:nth-child(2) { opacity: 0; }
        .menu-toggle.is-active .bar:nth-child(1) { transform: translateY(8px) rotate(45deg); }
        .menu-toggle.is-active .bar:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }
    }
</style>
