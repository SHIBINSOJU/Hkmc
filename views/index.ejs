<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="HKMC - Kerala's #1 Lifesteal Minecraft Server. Join the hardcore community today!">
    <title>HKMC | Kerala's Lifesteal Network</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;500;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- GLOBAL VARIABLES --- */
        :root {
            --primary: #ff1a1a;       /* Hardcore Red */
            --primary-dim: #8b0000;   /* Dark Red */
            --bg: #050505;            /* Deep Black */
            --card-bg: rgba(20, 20, 20, 0.6);
            --glass: 12px;
            --text-main: #ffffff;
            --text-muted: #aaaaaa;
        }

        /* --- BASE STYLES --- */
        * { box-sizing: border-box; }
        
        body {
            background-color: var(--bg);
            /* Subtle background pattern */
            background-image: 
                radial-gradient(circle at 50% 10%, rgba(139, 0, 0, 0.2) 0%, transparent 60%),
                url('https://www.transparenttextures.com/patterns/dark-matter.png');
            color: var(--text-main);
            font-family: 'Outfit', sans-serif;
            margin: 0;
            overflow-x: hidden;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* --- HERO SECTION --- */
        .hero {
            min-height: 85vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 20px;
            margin-top: 60px; /* Space for fixed navbar */
        }

        .hero-logo {
            width: 160px;
            margin-bottom: 20px;
            filter: drop-shadow(0 0 25px rgba(255, 26, 26, 0.4));
            animation: float 4s ease-in-out infinite;
        }

        .server-status {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(255, 255, 255, 0.05);
            padding: 6px 16px;
            border-radius: 50px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            font-size: 0.9rem;
            margin-bottom: 20px;
            backdrop-filter: blur(5px);
        }

        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background-color: #00ff00;
            box-shadow: 0 0 10px #00ff00;
        }
        .status-dot.offline { background-color: #ff0000; box-shadow: 0 0 10px #ff0000; }

        h1 {
            font-size: 3.5rem;
            line-height: 1.1;
            margin: 10px 0;
            text-transform: uppercase;
            font-weight: 900;
            letter-spacing: 2px;
            background: -webkit-linear-gradient(#fff, #ff4d4d);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        p.subtitle {
            color: var(--text-muted);
            font-size: 1.2rem;
            margin-bottom: 40px;
            max-width: 600px;
        }

        /* --- IP COPY BUTTON --- */
        .ip-wrapper {
            position: relative;
            display: inline-block;
        }

        .copy-btn {
            background: rgba(255, 255, 255, 0.08);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.2);
            padding: 15px 40px;
            font-size: 1.3rem;
            font-weight: 700;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .copy-btn:hover {
            background: var(--primary);
            border-color: var(--primary);
            box-shadow: 0 0 30px rgba(255, 26, 26, 0.4);
            transform: translateY(-3px);
        }

        .copy-btn i { font-size: 1rem; opacity: 0.7; }

        .player-count {
            margin-top: 15px;
            font-size: 0.9rem;
            color: #888;
        }

        /* --- FEATURES GRID --- */
        .features-section {
            padding: 0 5%;
            margin-top: -50px; /* Overlap effect */
            margin-bottom: 100px;
            position: relative;
            z-index: 2;
        }

        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .card {
            background: var(--card-bg);
            backdrop-filter: blur(var(--glass));
            -webkit-backdrop-filter: blur(var(--glass));
            padding: 35px 25px;
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.08);
            text-align: left;
            transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 3px;
            background: linear-gradient(90deg, transparent, var(--primary), transparent);
            opacity: 0;
            transition: 0.4s;
        }

        .card:hover {
            transform: translateY(-10px);
            border-color: rgba(255, 26, 26, 0.3);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5);
        }

        .card:hover::before { opacity: 1; }

        .card-icon {
            font-size: 2rem;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .card h3 {
            margin: 0 0 10px 0;
            font-size: 1.4rem;
            color: #fff;
        }

        .card p {
            margin: 0;
            color: var(--text-muted);
            line-height: 1.6;
            font-size: 0.95rem;
        }

        /* Special style for Leaderboard Card */
        .card.leaderboard {
            border: 1px solid rgba(255, 215, 0, 0.2);
            background: linear-gradient(145deg, rgba(20,20,20,0.8), rgba(40,35,10,0.4));
        }
        .card.leaderboard .card-icon { color: #ffd700; }
        .stat-row {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
            font-weight: bold;
            background: rgba(0,0,0,0.3);
            padding: 10px;
            border-radius: 8px;
        }

        /* --- ANIMATIONS --- */
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-15px); }
            100% { transform: translateY(0px); }
        }

        /* Mobile Adjustments */
        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            .hero-logo { width: 120px; }
            .grid-container { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <%- include('partials/navbar', { page: 'home' }) %>

    <div class="hero">
        <img src="/images/logo.png" alt="HKMC Logo" class="hero-logo">
        
        <div class="server-status">
            <span class="status-dot <%= online ? '' : 'offline' %>"></span>
            <%= online ? 'Server Online' : 'Server Offline' %>
        </div>

        <h1>Hardcore Kerala<br>Minecraft Community</h1>
        <p class="subtitle">Join Kerala's #1 Lifesteal SMP. Grind, Kill, Survive.</p>

        <div class="ip-wrapper">
            <button class="copy-btn" onclick="copyIP()" id="ipBtn">
                <span>play.hkmc.fun</span>
                <i class="fas fa-copy"></i>
            </button>
            <div class="player-count">
                <i class="fas fa-users"></i> 
                Currently Online: <strong><%= players %> / <%= max %></strong>
            </div>
        </div>
    </div>

    <div class="features-section">
        <div class="grid-container">
            
            <div class="card">
                <div class="card-icon"><i class="fas fa-bolt"></i></div>
                <h3>Low Latency</h3>
                <p>Hosted on premium Mumbai Ryzen nodes. Experience 0% lag and instant hit-registration for intense PvP.</p>
            </div>

            <div class="card">
                <div class="card-icon"><i class="fas fa-heart"></i></div>
                <h3>Lifesteal Mechanics</h3>
                <p>Kill players to steal hearts. Use <strong>/withdraw</strong> to craft hearts. If you reach 0 hearts, you are banned.</p>
            </div>

            <div class="card">
                <div class="card-icon"><i class="fas fa-shield-alt"></i></div>
                <h3>Advanced Anti-Cheat</h3>
                <p>Our custom GrimAC config ensures fair play. Hackers are detected and banned instantly by our staff.</p>
            </div>

            <div class="card leaderboard">
                <div class="card-icon"><i class="fas fa-crown"></i></div>
                <h3>Top Killer</h3>
                
                <% if(leaderboard && leaderboard.length > 0) { %>
                    <div style="margin-top: 10px;">
                        <span style="font-size: 1.3rem; font-weight: 800; color: white;">
                            <%= leaderboard[0].username %>
                        </span>
                        <div class="stat-row">
                            <span style="color:#ff5555">⚔️ <%= leaderboard[0].kills %> Kills</span>
                            <span style="color:#ff5555">❤️ <%= leaderboard[0].hearts %> Hearts</span>
                        </div>
                    </div>
                <% } else { %>
                    <p>No stats available yet.</p>
                <% } %>
            </div>

        </div>
    </div>

    <%- include('partials/footer') %>

    <script>
        function copyIP() {
            const ip = "play.hkmc.fun";
            navigator.clipboard.writeText(ip).then(() => {
                const btn = document.getElementById("ipBtn");
                const originalHTML = btn.innerHTML;
                
                // Visual feedback
                btn.style.borderColor = "#00ff00";
                btn.innerHTML = "<span>IP Copied!</span> <i class='fas fa-check'></i>";
                
                setTimeout(() => {
                    btn.style.borderColor = "rgba(255, 255, 255, 0.2)";
                    btn.innerHTML = originalHTML;
                }, 2000);
            }).catch(err => {
                console.error('Failed to copy: ', err);
            });
        }
    </script>

</body>
</html>
