<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stats - HKMC</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;500;800&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <style>
        /* --- GLOBAL VARIABLES --- */
        :root { --primary: #ff1a1a; --glass: 12px; }
        
        /* CLEAN BODY: No background color here (Navbar handles it) */
        body {
            font-family: 'Outfit', sans-serif;
            margin: 0;
            /* Layout fixes are inherited from Navbar now */
        }

        .container {
            max-width: 1100px;
            /* Added extra top margin because Navbar is floating now */
            margin: 120px auto 50px; 
            padding: 20px;
            flex: 1; /* Pushes footer to bottom */
        }

        /* --- HEADINGS --- */
        .page-header {
            text-align: center;
            margin-bottom: 50px;
        }
        .page-header h1 {
            font-size: 3rem;
            text-transform: uppercase;
            background: -webkit-linear-gradient(#fff, #ff4d4d);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin: 0;
        }

        /* --- LIVE PLAYERS STYLES --- */
        .live-section {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(0, 255, 0, 0.1); /* Green tint for "Live" */
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 40px;
            text-align: center;
        }

        .live-section h3 {
            color: #00ff00; /* Bright Green */
            margin-top: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        /* The glowing green dot */
        .pulse-dot {
            width: 10px;
            height: 10px;
            background-color: #00ff00;
            border-radius: 50%;
            box-shadow: 0 0 10px #00ff00;
            animation: pulse 1.5s infinite;
        }

        .online-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        /* The individual player tag */
        .player-chip {
            background: rgba(255, 255, 255, 0.05);
            padding: 8px 15px 8px 8px; /* Extra padding on right for text */
            border-radius: 30px;
            display: flex;
            align-items: center;
            gap: 10px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: 0.2s;
        }

        .player-chip:hover {
            background: rgba(255, 255, 255, 0.15);
            transform: translateY(-2px);
        }

        .player-chip img {
            width: 30px;
            height: 30px;
            border-radius: 50%;
        }

        .player-chip span {
            font-weight: bold;
            color: #ddd;
        }

        @keyframes pulse {
            0% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.5; transform: scale(1.2); }
            100% { opacity: 1; transform: scale(1); }
        }

        /* --- PODIUM (TOP 3) --- */
        .podium {
            display: flex;
            justify-content: center;
            align-items: flex-end;
            gap: 20px;
            margin-bottom: 60px;
            flex-wrap: wrap;
        }

        .podium-card {
            background: rgba(20, 20, 20, 0.6);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            width: 200px;
            position: relative;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: 0.3s;
        }

        .podium-card:hover { transform: translateY(-10px); }

        /* Rank Colors */
        .rank-1 { border-color: #ffd700; box-shadow: 0 0 20px rgba(255, 215, 0, 0.2); height: 280px; order: 2; }
        .rank-2 { border-color: #c0c0c0; box-shadow: 0 0 20px rgba(192, 192, 192, 0.2); height: 240px; order: 1; }
        .rank-3 { border-color: #cd7f32; box-shadow: 0 0 20px rgba(205, 127, 50, 0.2); height: 220px; order: 3; }

        .crown { font-size: 2rem; margin-bottom: 10px; display: block; }
        .rank-1 .crown { color: #ffd700; }
        .rank-2 .crown { color: #c0c0c0; }
        .rank-3 .crown { color: #cd7f32; }

        .avatar {
            width: 60px;
            border-radius: 50%;
            margin-bottom: 10px;
            border: 2px solid rgba(255,255,255,0.2);
        }

        .stat-badge {
            background: rgba(255, 0, 0, 0.2);
            color: #ff5555;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.9rem;
            margin-top: 5px;
            display: inline-block;
        }

        /* --- TABLE --- */
        .table-container {
            background: rgba(20, 20, 20, 0.6);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            text-align: left;
        }

        th, td {
            padding: 15px 25px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        th {
            background: rgba(255, 26, 26, 0.1);
            color: #ff1a1a;
            text-transform: uppercase;
            font-size: 0.9rem;
            letter-spacing: 1px;
        }

        tr:hover { background: rgba(255, 255, 255, 0.03); }

        .rank-num { font-weight: bold; color: #888; }
        .heart-icon { color: #ff1a1a; margin-right: 5px; }

        /* Mobile Fix */
        @media (max-width: 768px) {
            .podium { flex-direction: column; align-items: center; }
            .rank-1, .rank-2, .rank-3 { order: unset; height: auto; width: 100%; }
            th, td { padding: 10px; font-size: 0.9rem; }
            .hide-mobile { display: none; } /* Hide Balance/Deaths on small screens */
        }
    </style>
</head>
<body>

    <%- include('partials/navbar', { page: 'leaderboard' }) %>

    <div class="container">
        
        <div class="page-header">
            <h1>Top Killers</h1>
            <p style="color: #888;">The deadliest players in Kerala</p>
        </div>

        <div class="live-section">
            <h3>
                <span class="pulse-dot"></span> 
                Online Now (<%= livePlayers.length %>)
            </h3>

            <% if (livePlayers.length > 0) { %>
                <div class="online-grid">
                    <% livePlayers.forEach(p => { %>
                        <div class="player-chip">
                            <img src="https://mc-heads.net/avatar/<%= p.name %>" alt="<%= p.name %>">
                            <span><%= p.name %></span>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p style="color: #666; font-style: italic;">No players online... Time to join!</p>
            <% } %>
        </div>

        <hr style="border-color: rgba(255,255,255,0.1); margin: 40px 0;">

        <div class="podium">
            <% if (players.length > 1) { %>
                <div class="podium-card rank-2">
                    <i class="fas fa-crown crown"></i>
                    <img src="https://mc-heads.net/avatar/<%= players[1].username %>" class="avatar">
                    <h2><%= players[1].username %></h2>
                    <div class="stat-badge"><%= players[1].kills %> Kills</div>
                </div>
            <% } %>

            <% if (players.length > 0) { %>
                <div class="podium-card rank-1">
                    <i class="fas fa-crown crown"></i>
                    <img src="https://mc-heads.net/avatar/<%= players[0].username %>" class="avatar">
                    <h1 style="font-size: 2rem; margin: 10px 0;"><%= players[0].username %></h1>
                    <div class="stat-badge" style="font-size: 1.2rem;">⚔️ <%= players[0].kills %></div>
                </div>
            <% } %>

            <% if (players.length > 2) { %>
                <div class="podium-card rank-3">
                    <i class="fas fa-crown crown"></i>
                    <img src="https://mc-heads.net/avatar/<%= players[2].username %>" class="avatar">
                    <h2><%= players[2].username %></h2>
                    <div class="stat-badge"><%= players[2].kills %> Kills</div>
                </div>
            <% } %>
        </div>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Player</th>
                        <th>Kills</th>
                        <th class="hide-mobile">Deaths</th>
                        <th>Hearts</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (players.length === 0) { %>
                        <tr>
                            <td colspan="5" style="text-align: center; color: #888;">No data yet. Join the server to be the first!</td>
                        </tr>
                    <% } else { %>
                        <% players.forEach((player, index) => { %>
                            <tr>
                                <td class="rank-num">#<%= index + 1 %></td>
                                <td style="display: flex; align-items: center; gap: 10px;">
                                    <img src="https://mc-heads.net/avatar/<%= player.username %>" width="30" style="border-radius: 5px;">
                                    <strong><%= player.username %></strong>
                                </td>
                                <td style="color: #ff5555; font-weight: bold;"><%= player.kills %></td>
                                <td class="hide-mobile" style="color: #888;"><%= player.deaths %></td>
                                <td><i class="fas fa-heart heart-icon"></i> <%= player.hearts %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

    </div>

    <%- include('partials/footer') %>

</body>
</html>
