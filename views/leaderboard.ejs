<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta property="og:title" content="HKMC Stats - Top Killers">
<meta property="og:description" content="Check live stats, K/D ratios, and player rankings on the HKMC Minecraft Server.">
<meta property="og:image" content="https://cdn.discordapp.com/attachments/123.../logo.png"> <meta property="og:url" content="https://hkmc-stats.onrender.com">
<meta name="theme-color" content="#ff1a1a">

    <title>Stats - HKMC</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;500;800&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <style>
        /* --- GLOBAL VARIABLES --- */
        :root { --primary: #ff1a1a; --glass: 12px; }
        
        body {
            font-family: 'Outfit', sans-serif;
            margin: 0;
            background-color: #0a0a0a;
            color: white;
            touch-action: manipulation; /* Disables double-tap zoom */
        }

        /* ZOOM FIX: Input font size 16px+ prevents iOS zoom */
        input, select, textarea {
            font-size: 16px !important; 
            touch-action: manipulation;
        }

        .container {
            max-width: 1100px;
            margin: 120px auto 50px; 
            padding: 20px;
            flex: 1; 
        }

        /* --- HEADINGS --- */
        .page-header {
            text-align: center;
            margin-bottom: 50px;
        }
        .page-header h1 {
            font-size: 3rem;
            text-transform: uppercase;
            background: -webkit-linear-gradient(#fff, #ff4d4d);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin: 0;
        }

        /* --- STICKY SEARCH BAR (Mobile Upgrade) --- */
        .search-container {
            position: sticky;
            top: 80px; /* Sticks below the navbar */
            z-index: 100;
            background: rgba(10, 10, 10, 0.85); /* Semi-transparent background */
            backdrop-filter: blur(10px);
            padding: 15px 0;
            margin-bottom: 20px;
            display: flex;
            justify-content: flex-end;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .search-input {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            padding: 12px 20px;
            border-radius: 30px;
            color: white;
            font-family: 'Outfit', sans-serif;
            width: 100%;
            max-width: 100%; /* Full width on mobile */
            outline: none;
            transition: 0.3s;
        }
        .search-input:focus {
            background: rgba(255, 255, 255, 0.2);
            border-color: #ff5555;
            box-shadow: 0 0 15px rgba(255, 85, 85, 0.3);
        }

        /* --- LIVE PLAYERS SECTION --- */
        .live-section {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(0, 255, 0, 0.1);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 40px;
            text-align: center;
        }

        .live-section h3 {
            color: #00ff00;
            margin-top: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .pulse-dot {
            width: 10px;
            height: 10px;
            background-color: #00ff00;
            border-radius: 50%;
            box-shadow: 0 0 10px #00ff00;
            animation: pulse 1.5s infinite;
        }

        .online-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        .player-chip {
            background: rgba(255, 255, 255, 0.05);
            padding: 8px 15px 8px 8px;
            border-radius: 30px;
            display: flex;
            align-items: center;
            gap: 10px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: 0.2s;
        }
        .player-chip img { width: 30px; height: 30px; border-radius: 50%; }
        .player-chip span { font-weight: bold; color: #ddd; }

        @keyframes pulse {
            0% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.5; transform: scale(1.2); }
            100% { opacity: 1; transform: scale(1); }
        }

        /* --- TABLE STYLES --- */
        .table-container {
            background: rgba(20, 20, 20, 0.6);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            overflow: hidden; /* Important for border radius */
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        table { width: 100%; border-collapse: collapse; text-align: left; }
        th, td { padding: 15px 25px; border-bottom: 1px solid rgba(255, 255, 255, 0.05); }
        th { background: rgba(255, 26, 26, 0.1); color: #ff1a1a; text-transform: uppercase; font-size: 0.9rem; letter-spacing: 1px; }
        tr:hover { background: rgba(255, 255, 255, 0.03); }

        .rank-num { font-weight: bold; color: #888; }
        .heart-icon { color: #ff1a1a; margin-right: 5px; }
        
        a.player-link {
            text-decoration: none;
            color: white;
            display: flex; 
            align-items: center; 
            gap: 10px;
            transition: 0.2s;
        }
        a.player-link:hover { color: #ff5555; transform: translateX(5px); }

        /* --- PEAK MOBILE UI (The Magic Part) --- */
        @media (max-width: 768px) {
            /* 1. Hide the Table Headers */
            thead { display: none; }

            /* 2. Turn Rows into Cards */
            tbody tr {
                display: flex;
                flex-wrap: wrap;
                background: rgba(255, 255, 255, 0.05);
                margin-bottom: 15px;
                border-radius: 15px;
                padding: 15px;
                border: 1px solid rgba(255, 255, 255, 0.1);
                position: relative;
                box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            }

            /* 3. Rank Number (Top Right Corner) */
            td.rank-num {
                position: absolute;
                top: 10px;
                right: 15px;
                font-size: 1.5rem;
                opacity: 0.2;
                border: none;
                padding: 0;
            }

            /* 4. Player Name & Avatar (Full Width Header) */
            td:nth-child(2) {
                width: 100%;
                padding: 0 0 15px 0;
                border-bottom: 1px solid rgba(255, 255, 255, 0.1);
                margin-bottom: 10px;
                font-size: 1.2rem;
            }

            /* 5. Stats Grid (Kills, K/D, Hearts) */
            td {
                border: none;
                padding: 5px 0;
                font-size: 0.95rem;
                flex: 1; /* Space them evenly */
                text-align: center;
                display: flex;
                flex-direction: column;
                align-items: center;
                color: #ddd;
            }

            /* Add Icons/Labels above numbers using CSS */
            td:nth-child(3)::before { content: "⚔️ Kills"; font-size: 0.7rem; color: #888; margin-bottom: 2px; }
            td:nth-child(5)::before { content: "🎯 K/D"; font-size: 0.7rem; color: #888; margin-bottom: 2px; }
            td:nth-child(6)::before { content: "❤️ Health"; font-size: 0.7rem; color: #888; margin-bottom: 2px; }

            /* Hide the "Deaths" column on mobile to save space */
            .hide-mobile { display: none !important; }
            
            /* Remove hover effect on mobile */
            tr:hover { background: rgba(255, 255, 255, 0.05); }
        }
    </style>
</head>
<body>

    <%- include('partials/navbar', { page: 'leaderboard' }) %>

    <div class="container">
        
        <div class="page-header">
            <h1>Top Killers</h1>
            <p style="color: #888;">The deadliest players in Kerala</p>
        </div>

        <div class="live-section">
            <h3>
                <span class="pulse-dot"></span> 
                Online Now (<%= livePlayers.length %>)
            </h3>

            <% if (livePlayers.length > 0) { %>
                <div class="online-grid">
                    <% livePlayers.forEach(p => { %>
                        <div class="player-chip">
                            <img src="https://mc-heads.net/avatar/<%= p.name %>" alt="<%= p.name %>">
                            <span><%= p.name %></span>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p style="color: #666; font-style: italic;">No players online... Time to join!</p>
            <% } %>
        </div>

        <hr style="border-color: rgba(255,255,255,0.1); margin: 40px 0;">

        <div class="search-container">
            <input type="text" id="searchInput" class="search-input" placeholder="🔍 Search player...">
        </div>

        <div class="table-container">
            <table id="statsTable">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Player</th>
                        <th>Kills</th>
                        <th class="hide-mobile">Deaths</th>
                        <th>K/D</th> 
                        <th>Hearts</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (players.length === 0) { %>
                        <tr>
                            <td colspan="6" style="text-align: center; color: #888;">No data yet. Join the server to be the first!</td>
                        </tr>
                    <% } else { %>
                        <% players.forEach((player, index) => { %>
                            <tr>
                                <td class="rank-num">#<%= index + 1 %></td>
                                <td>
                                    <a href="/player/<%= player.username %>" class="player-link">
                                        <img src="https://mc-heads.net/avatar/<%= player.username %>" width="30" style="border-radius: 5px;">
                                        <strong><%= player.username %></strong>
                                    </a>
                                </td>
                                <td style="color: #ff5555; font-weight: bold;"><%= player.kills %></td>
                                <td class="hide-mobile" style="color: #888;"><%= player.deaths %></td>
                                
                                <td style="color: #55aaff;">
                                    <%= (player.kills / (player.deaths > 0 ? player.deaths : 1)).toFixed(2) %>
                                </td>

                                <td><i class="fas fa-heart heart-icon hide-desktop"></i> <%= player.hearts %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

    </div>

    <%- include('partials/footer') %>

    <script>
        document.getElementById('searchInput').addEventListener('keyup', function() {
            let filter = this.value.toLowerCase();
            let rows = document.querySelectorAll('#statsTable tbody tr');

            rows.forEach(row => {
                let nameCell = row.querySelector('.player-link strong');
                if (nameCell) {
                    let text = nameCell.textContent || nameCell.innerText;
                    if (text.toLowerCase().indexOf(filter) > -1) {
                        row.style.display = "flex"; /* Important: keep flex for card view */
                        if (window.innerWidth > 768) row.style.display = "table-row"; /* Reset for desktop */
                    } else {
                        row.style.display = "none";
                    }
                }
            });
        });
    </script>

</body>
</html>
